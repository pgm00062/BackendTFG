spring.application.name=BackendTFG

# Puerto en el que correrá la app
server.port=8080

spring.datasource.url=jdbc:mysql://localhost:3306/freelance_app?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Europe/Madrid
spring.datasource.username=root
spring.datasource.password=emU.tuw981
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración para mantener los datos
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Configuración para ejecutar scripts SQL solo si las tablas están vacías
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=embedded
spring.sql.init.schema-locations=classpath:schema.sql

# Logging adicional para diagnosticar problemas
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.springframework.orm.jpa=DEBUG

